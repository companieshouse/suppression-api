environment:
  cdnHost: cdn.test.local
  chEmail: appeals.ch.fake@gmail.com
  
email:
  producer:
    appId: suppression-api

kafka:
  broker:
    addr: kafka-test:1111
  config:
    acks: WAIT_FOR_ALL
    retries: 10
    isRoundRobin: false
    
server:
  port: 9000

spring:
  data:
    mongodb:
      uri: "mongodb://localhost/test"

suppression:
  payment:
    amount: 32
